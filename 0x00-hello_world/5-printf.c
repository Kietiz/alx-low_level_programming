#include <stdio.h>
#include <unistd.h>
/**
 * main - prints a string using print
 * Description: Prints a string of characters when successful
 * Return: int (0) when successfull and no errors
 */
int main(void)
{
	printf(\"with proper grammar, but the outcome is a piece of art,");
	return (0);
}
