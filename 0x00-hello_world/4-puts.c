#include <stdio.h>

/**
 * 
 * main - prints a string using puts function
 * description - this program prints strings followed by new line 
 * return: int 0 when done and there are no errors
 */

int main(void)
{
	puts("\"Programming is like building a multilingual puzzle");
	return (0);
}
