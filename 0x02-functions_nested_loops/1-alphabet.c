#include "main.h"

/**
 * print_alphabet - prints alphabets in small letters
 * Description: Uses headers
 * Return: always 0
 */

void print_alphabet(void)
{
	char c;

	for (c = 'a' ; c <= 'z' ; c++)
		_putchar(x);
	_putchar('\n');
}
